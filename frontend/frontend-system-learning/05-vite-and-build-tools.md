# 05 Vite 与构建工具

> 层级：第四层工程化层

## 问题背景
为什么很多人把 Vite 当 React/Vue？

## 核心解释
Vite 是开发服务器 + 构建工具，不是 UI 框架。
它负责启动、热更新、构建打包；不负责组件模型。

## 技术关系
Vite 可搭配 React、Vue，也可搭配 Vanilla TS。

## 学习建议
先理解 `package.json scripts` 与 `vite.config.ts` 角色。

## 小结
Vite 属于工具层，React/Vue 属于框架层。
