export const metadata = {
  title: "MDX Demo",
};

# 我的第一个 MDX 页面

这是普通的 **Markdown** 内容。

- 可以写列表
- 可以写链接
- 可以写代码

下面这个区域直接使用了 JSX：

<div className="mt-6 rounded-2xl bg-black p-6 text-white">
  我是写在 MDX 里的 JSX
</div>

表达式也可以运行：1 + 2 = {1 + 2}