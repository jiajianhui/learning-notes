name: prisma-crud-demo

services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: backend_learning
      POSTGRES_PASSWORD: backend_learning_password
      POSTGRES_DB: backend_learning
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
