# 11. 数据关系和事务：多张表怎样一起保持正确

## 问题背景

文章和标签不是一张表里的简单字段。

一篇文章可以有多个标签，一个标签也可以属于多篇文章，这是典型的多对多关系。

```text
articles
<-> article_tags
<-> tags
```

---

## 核心解释

### 1. 常见关系

```text
一对一
-> 一个用户对应一个个人设置

一对多
-> 一个作者对应多篇文章

多对多
-> 一篇文章有多个标签，一个标签属于多篇文章
```

多对多通常需要中间表。

### 2. 文章和标签的中间表

```sql
CREATE TABLE article_tags (
  article_id BIGINT NOT NULL REFERENCES articles(id) ON DELETE CASCADE,
  tag_id BIGINT NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
  PRIMARY KEY (article_id, tag_id)
);
```

联合主键防止同一篇文章重复添加同一个标签。

外键防止关联不存在的文章或标签。

`ON DELETE CASCADE` 在这里表示文章或标签删除后，对应的关联记录一起删除，不是把另一侧的文章或标签也删除。

### 3. JOIN 把关联数据查回来

```sql
SELECT tags.id, tags.name, tags.slug
FROM tags
JOIN article_tags ON article_tags.tag_id = tags.id
WHERE article_tags.article_id = $1;
```

JOIN 不是把表永久合并，而是在一次查询里按关系组合结果。

---

## 为什么需要事务

假设创建文章需要：

```text
1. 插入 articles
2. 插入三条 article_tags
```

如果第一步成功、第二步失败，就会留下不完整结果。

事务让它们：

```text
全部成功 -> COMMIT
任何一步失败 -> ROLLBACK
```

### 使用 pg 执行事务

事务中的所有 SQL 必须使用同一个 client：

```ts
const client = await pool.connect();

try {
  await client.query("BEGIN");

  const articleResult = await client.query(
    "INSERT INTO articles (...) VALUES (...) RETURNING id",
    values,
  );

  await client.query(
    "INSERT INTO article_tags (article_id, tag_id) VALUES ($1, $2)",
    [articleResult.rows[0].id, tagId],
  );

  await client.query("COMMIT");
} catch (error) {
  await client.query("ROLLBACK");
  throw error;
} finally {
  client.release();
}
```

不能在同一事务里连续使用不确定连接的 `pool.query()`。

---

## 什么时候需要事务

先问：

```text
这些数据库操作是否必须作为一个整体成功？
```

Mini CMS 中可能需要：

- 创建文章并保存标签关系。
- 更新文章并替换全部标签关系。
- 发布文章并记录相关状态。

单条普通查询不需要为了形式套事务。

---

## 常见误区

- 把标签数组直接长期塞进文章字符串字段，失去关系和约束。
- 建了外键，却没想清楚删除时应该限制、置空还是级联。
- JOIN 后出现重复文章，不理解一对多结果会扩展行数。
- 事务中使用不同数据库连接。
- 忘记在 `finally` 中释放 client。
- 所有查询都开事务，增加复杂度却没有一致性收益。

---

## 小结

```text
外键保护关系存在
中间表表达多对多
JOIN 在查询时组合关联数据
事务保证一组操作一起成功或失败
```

这一层完成后，Mini CMS 才从单表 CRUD 进入真正的关系型业务。
