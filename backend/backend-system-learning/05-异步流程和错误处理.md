# 05. 异步和错误：数据库不会立刻返回结果

## 问题背景

读取变量通常立刻完成，但网络请求、文件读取和数据库查询都需要等待。

后端的大量代码都是：

```text
等待某个外部结果
-> 成功后继续
-> 失败时交给统一错误处理
```

---

## 核心解释

### 1. 为什么后端操作经常是异步的

Express 和 PostgreSQL 是两个不同进程。执行 SQL 时，Node.js 要等待数据库返回结果。

```ts
const article = await findArticle(articleId);
```

`await` 不是让整个服务器停止，而是让当前异步函数等待 Promise 的结果。

这里先把 `findArticle()` 理解成“需要等待的查询函数”。它怎样通过 `pg` 执行 SQL，会在第 08 章再展开。

### 2. `async` 函数会返回 Promise

```ts
async function getArticleTitle(id: number) {
  const article = await findArticle(id);
  return article.title;
}
```

调用方也需要等待：

```ts
const title = await getArticleTitle(1);
```

### 3. 业务错误和系统错误不同

```text
业务错误
-> 文章不存在、slug 重复、没有登录

系统错误
-> 数据库断开、代码异常、配置缺失
```

业务错误应该返回稳定状态码和可理解信息；未知系统错误通常返回 500，并在服务器记录细节。

---

## 统一错误处理

可以先定义一个带状态码的错误：

```ts
class AppError extends Error {
  constructor(
    public statusCode: number,
    public code: string,
    message: string,
  ) {
    super(message);
  }
}
```

业务代码抛出错误：

```ts
if (!article) {
  throw new AppError(404, "ARTICLE_NOT_FOUND", "文章不存在");
}
```

最后由错误处理中间件统一返回：

```ts
app.use((error, _request, response, _next) => {
  const statusCode = error.statusCode ?? 500;

  response.status(statusCode).json({
    error: {
      code: error.code ?? "INTERNAL_ERROR",
      message: statusCode === 500 ? "服务器内部错误" : error.message,
    },
  });
});
```

错误处理中间件必须保留四个参数，通常注册在所有路由之后。

---

## Express 5 中的异步错误

当前 Express 5 可以把异步 route handler 中抛出的错误或被拒绝的 Promise 交给错误处理中间件：

```ts
articleRouter.get("/:id", async (request, response) => {
  const article = await findArticle(Number(request.params.id));

  if (!article) {
    throw new AppError(404, "ARTICLE_NOT_FOUND", "文章不存在");
  }

  response.json({ data: article });
});
```

如果维护旧 Express 项目，需要先确认它使用的版本和异步错误写法。

---

## Mini CMS 中用在哪里

- 查询文章时等待 PostgreSQL。
- 创建文章时捕获唯一约束冲突。
- 未找到文章时返回 404。
- 数据库故障时记录错误并返回 500。
- 所有模块共用一种错误结构。

---

## 常见误区

- 忘记 `await`，拿到的是 Promise，不是查询结果。
- 在每个 route 中复制一套 `try/catch` 和响应格式。
- 把数据库原始错误和堆栈直接返回给浏览器。
- 错误处理中间件放在路由之前。
- 已经发送响应后又继续抛错或再次发送响应。

---

## 小结

后端不只处理成功流程。

```text
async / await 负责等待异步结果
throw 负责中断当前流程
统一错误中间件负责把错误变成稳定 HTTP 响应
日志负责保留服务器内部细节
```

错误路径清楚，接口才算真正可用。
