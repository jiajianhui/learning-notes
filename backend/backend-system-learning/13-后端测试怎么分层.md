# 13. 后端测试：Apifox、管理后台和自动化测试各管一层

## 问题背景

只在 Apifox 中调用成功一次，不能证明接口以后一直可靠。

只在管理后台点击，也很难覆盖缺少字段、重复 slug、未登录等错误条件。

后端验证应该分层：

```text
接口手动检查
产品流程检查
自动化回归测试
```

---

## 核心解释

### 1. Apifox：主动构造请求

适合：

- 快速查看请求和响应。
- 修改 headers、body 和 query。
- 检查错误参数和状态码。
- 在前端还没完成时验证 API。

不足：需要人工操作，修改代码后容易漏掉旧场景。

### 2. Next.js 管理后台：验证真实工作流

适合：

- 用户能否完成新建、编辑、发布和删除。
- 表单和 API 是否正确配合。
- loading、empty、error、success 是否完整。
- Cookie 登录是否能在浏览器里工作。

它是产品界面，不是专门的接口边界测试工具。

### 3. 自动化测试：稳定重复验证

```text
Vitest
-> 组织测试、断言结果

Supertest
-> 向 Express app 发送测试请求
```

每次修改代码都可以重新运行，不依赖人工点击。

---

## 测试层级

| 类型 | 主要验证什么 | Mini CMS 示例 |
|---|---|---|
| 单元测试 | 一个纯函数或业务规则 | slug 生成、状态转换 |
| 接口集成测试 | 路由、中间件、业务和数据库配合 | `POST /api/articles` |
| 端到端测试 | 浏览器中的完整流程 | 登录后创建并发布文章 |

第一轮优先接口集成测试，不急着搭完整浏览器 E2E。

---

## 为什么要把 `app` 和 `server` 分开

`app.ts` 导出 Express app：

```ts
export { app };
```

`server.ts` 才监听端口：

```ts
app.listen(port);
```

测试可以直接使用 app：

```ts
import request from "supertest";
import { app } from "../src/app.js";

it("returns health status", async () => {
  await request(app)
    .get("/api/health")
    .expect(200)
    .expect({ status: "ok" });
});
```

这里导入的 `request()` 是 Supertest 提供的测试函数，用来模拟客户端向 `app` 发请求；它不是 Express handler 收到的 `req` 对象。

不需要手动先运行端口 3001。

---

## 数据库测试要隔离

测试不能随意删除开发数据库的数据。

可以使用：

```text
独立测试数据库
-> 每个测试前准备数据
-> 每个测试后清理或回滚
```

测试必须可重复，不能依赖“数据库里刚好已经有一篇文章”。

---

## Mini CMS 优先测试什么

- 创建文章成功并返回 201。
- 缺少标题时返回校验错误。
- slug 重复时返回 409。
- 查询不存在文章时返回 404。
- 未登录时不能修改文章。
- 登录后能创建和发布文章。
- 删除标签不会误删文章。

不要为了覆盖率数字测试每一行。先测试最重要的成功、参数错误、数据冲突和无权限情况。

---

## 常见误区

- Apifox、产品界面和自动化测试验证的范围不同，不是三选一；分别用于探索接口、验证操作流程和防止回归。
- 自动化测试直接使用开发数据库，会污染或删除自己的开发数据；使用独立测试数据库。
- 测试依赖上一条测试留下的数据，执行顺序变化就会失败；每条测试自行准备并清理所需数据。
- 只测试成功流程，无法发现无权限、参数错误和重复数据等真实问题；核心失败情况也要覆盖。
- 为了测试而导出内部变量，会让测试依赖实现细节；接口测试优先检查请求后返回的状态码和 JSON。

---

## 小结

```text
Apifox 方便探索接口
管理后台验证真实产品流程
Supertest 重复验证 HTTP 行为
测试数据库保证自动化过程可隔离
```

三层一起使用，后端才既能开发、能操作，也能放心修改。
