# 项目协作说明

## 项目定位

- 这是个人技术学习笔记仓库，内容包括前端、后端、AI、GitHub 和工具链。
- 文档主要用于建立知识主线、完成配套练习和日后快速复习。
- 根目录 `README.md` 负责说明仓库结构；各专题目录的 `README.md` 是该专题的主要入口。

## 开始工作前

- 先阅读根目录 `README.md`，再阅读当前专题的 `README.md`。
- 修改具体章节前，检查相邻章节、目录索引和对应示例代码，避免只根据单个文件判断。
- 后端学习路线以 `backend/backend-system-learning/README.md` 为准，项目阶段以同目录的 `10-MiniCMS项目总览.md` 为准。
- 如果目录中存在更具体的 `AGENTS.md` 或 `AGENTS.override.md`，同时遵循其中的局部规则。

## 文档写作原则

- 默认使用中文，句子简短，先讲主线，再补必要细节。
- 先说明“它解决什么问题”，再说明“怎样做”；避免重复解释同一件事。
- 面向正在学习相关技术的读者，不假设读者已经熟悉术语。
- 不为了显得完整而堆砌概念、边界情况或暂时用不到的工程细节。
- 不写入课程和示例中没有实际练习过的实现；涉及练习范围时，以现有文档和代码为证据。
- 术语、阶段划分、文件名和代码必须与仓库现状一致。
- 除非用户明确要求，不随意修改既有标题、章节结构或扩大内容范围。
- 代码示例保持最小、可理解，并说明它处于完整流程的哪一步。

## 修改与验证

- 用户只要求判断、检查或解释时，先给出结论和依据，不直接修改文件。
- 用户要求修改或优化时，只改与当前目标直接相关的内容。
- 保留用户已有改动，不覆盖或整理无关文件。
- 文档修改后检查标题层级、链接、路径、命令和代码块是否正确。
- 代码修改前先查看对应项目的 README 和 `package.json`，完成后运行与改动相关的检查。

## Git 约定

- 按根目录 README 的约定，日常直接在 `main` 维护。
- 只有用户明确要求“提交代码”时才创建提交，并且只暂存本次任务涉及的文件。
- 提交信息使用 `<type>: <中文摘要>; 由AI提交🌟` 的格式。
